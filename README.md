Realer Estate API – Instagram Checkout & Smart Search

Base URL: https://YOUR-API-HOST.com
Auth: All /api/* routes require X-API-Key: <VC_API_KEY> header.

This service powers:

IG DM → Account → Stripe checkout flow

JSON Stripe checkout link for bots

(Optional) Smart property search endpoints (cache-first + external APIs)
